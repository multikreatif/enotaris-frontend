# Base URL of enotaris-services API (no trailing slash)
# Example: http://localhost:8080
NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional: base path for assets/links (e.g. if app is served under a subpath)
# NEXT_PUBLIC_BASE_PATH=

# Syncfusion license key (untuk chart di dashboard). Dapat dari https://www.syncfusion.com/account/downloads
# Jangan commit nilai asli ke repo; isi di .env.local
# NEXT_PUBLIC_SYNCFUSION_LICENSE=
